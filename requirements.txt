# DATect: Domoic Acid Forecasting System Requirements
# ====================================================
# Install with: uv pip install -r requirements.txt (10x faster)
# Or standard: pip install -r requirements.txt
# See docs/DEPENDENCIES.md for optional/experimental packages.

# Core Scientific Computing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0         # Parallel processing for model training

# High-Performance Data Processing (optional fallbacks in code)
polars>=0.20.0        # 2-50x faster than Pandas when available
duckdb>=0.10.0        # Fast Parquet reads when available
pyarrow>=14.0.0       # Parquet file format

# Machine Learning
xgboost>=2.0.0        # Primary model (linear/logistic are in sklearn)
torch>=2.0.0          # PyTorch backend for TFT/TCN/TabNet/GPyTorch
pytorch-forecasting>=1.0.0
pytorch-lightning>=2.0.0
gpytorch>=1.11
pytorch-tabnet>=4.1.0
torchmetrics>=0.11

# Data Visualization
plotly>=5.18.0

# Data Processing and I/O
xarray>=2024.1.0      # NetCDF satellite data (dataset-creation)
netcdf4>=1.6.0        # NetCDF handling
requests>=2.31.0      # HTTP client

# Scientific Analysis
scipy>=1.12.0         # Spectral analysis (visualizations)
numba>=0.59.0         # Optional: JIT speedups (graceful fallback)
tqdm>=4.66.0          # Progress bars
SALib>=1.4.7          # Sensitivity analysis (Sobol in visualizations)

# Date parsing (used by pandas)
python-dateutil>=2.8.2
